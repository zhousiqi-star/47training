# 部署教程 · 从本地文件到网上能访问的网址

目标：把这个文件夹变成一个别人能打开的网址。

**推荐顺序**：先做方案 A（5 分钟拿到网址），再做方案 B（以后更新更方便）。两个可以同时存在。

---

## 方案 A：Netlify Drop（最快，5 分钟）

不需要注册 GitHub，不需要装任何软件。

### 第 1 步：打开拖拽上传页

浏览器打开：**https://app.netlify.com/drop**

### 第 2 步：注册 / 登录

用邮箱或 Google 账号登录即可，免费。

### 第 3 步：把文件拖进去

⚠️ **关键**：打开你的文件夹

```
C:\Users\zsq\Documents\机器学习课程
```

选中**里面的所有文件和文件夹**（`index.html`、`assets`、`data`、`videos.html` …），
**全选后一起拖**到网页上的虚线框里。

> 不要拖整个「机器学习课程」文件夹本身，否则网址会变成
> `https://xxx.netlify.app/机器学习课程/`，路径会错。

**全选快捷键**：在文件夹里按 `Ctrl + A`。

#### 更省事：直接拖 zip

我已经打包好一个文件：

```
C:\Users\zsq\Documents\47fitness.zip
```

它内部结构是 `index.html` 在根目录，**直接把这个 zip 拖进 Netlify Drop 就行**，
不用全选、不会拖错层级。

> 注意：改过本地文件后这个 zip 就是旧的，需要重新打包（或者改用方案 B，就不用管 zip 了）。

### 第 4 步：等几秒，拿到网址

拖完等 10–30 秒，页面会给你一个网址，长这样：

```
https://sparkly-panda-12345.netlify.app
```

点开就能用，手机也能打开。

### 第 5 步（可选）：改个好记的网址

站点设置 → **Change site name** → 填一个好记的名字，比如 `47fitness`，
网址就变成 `https://47fitness.netlify.app`。

### 以后怎么更新？

改完本地文件后，**重新拖一次**（Netlify 会覆盖旧版本）。

缺点是每次都要手动拖。想省事就做方案 B。

---

## 方案 B：GitHub + Cloudflare Pages（推荐长期用）

配好之后，**以后更新只要在网页上改一个文件，网站自动更新**。

### 第 1 步：注册 GitHub

https://github.com/signup —— 邮箱 + 密码，免费。

### 第 2 步：新建仓库

1. 登录后点右上角 **+** → **New repository**
2. Repository name 填 `fitness-site`（随便起）
3. 选 **Public**（公开，免费额度够用）
4. **不要**勾 "Add a README file"
5. 点 **Create repository**

### 第 3 步：把文件传上去

在新建的仓库页面：

1. 点 **uploading an existing file**（或 **Add file** → **Upload files**）
2. 打开本地文件夹 `C:\Users\zsq\Documents\机器学习课程`
3. 按 `Ctrl + A` 全选**里面的内容**，拖到网页的上传区
4. 等进度条走完（约 10–60 秒）
5. 下方 Commit message 随便写，比如 `first upload`
6. 点 **Commit changes**

传完后仓库里应该能直接看到 `index.html`、`style.css`、`exercises.js` 等，**`index.html` 必须在最外层**（不能套在文件夹里）。

> `打开网站.bat` 和 `README.md` 传上去也没关系，不影响网站运行。

### 第 4 步：注册 Cloudflare 并连接仓库

1. https://dash.cloudflare.com/sign-up 注册（免费）
2. 左侧菜单 → **Workers & Pages** → **Create** → **Pages** 标签 → **Connect to Git**
3. 授权 Cloudflare 访问你的 GitHub
4. 选中刚才的 `fitness-site` 仓库 → **Begin setup**
5. 构建设置按下面填：

| 设置项 | 填什么 |
|---|---|
| Framework preset | **None** |
| Build command | **留空** |
| Build output directory | **`/`** |

6. 点 **Save and Deploy**

等 30 秒左右，会给你一个网址：

```
https://fitness-site.pages.dev
```

### 第 5 步：以后怎么更新（重点）

1. 打开 GitHub 上这个仓库
2. 点进 `videos.js`（或任何要改的文件）
3. 点右上角的**铅笔图标** ✏️
4. 直接在网页里改
5. 拉到最下面点 **Commit changes**
6. 等约 30 秒，网址上的内容自动更新

**这就是你以后的日常操作，不需要在自己电脑上做任何事。**

### 用录入工具加视频的完整流程

1. 打开线上网址的 `videos.html`，点右上角「＋ 添加视频」
2. 填表 → 拖封面图 → 点「复制代码」
3. 到 GitHub 打开 `videos.js` → 点铅笔 → 粘贴 → Commit
4. 等 30 秒，新视频出现

---

## 方案 C：Vercel（备选）

和方案 B 几乎一样：

1. https://vercel.com/signup 用 GitHub 登录
2. **Add New** → **Project** → 选仓库 → **Import**
3. Framework Preset 选 **Other**，Build Command 和 Output Directory 都留空
4. **Deploy**

---

## 上线后自测清单

打开你的网址，挨个点一遍：

- [ ] 首页：6 个大卡片能点，搜索框能出建议
- [ ] 搜索页：搜「练背」有结果，筛选器能勾
- [ ] 详情页：点任意动作，步骤/错误/提示都显示
- [ ] 视频页：封面条能左右滑，点封面能预览
- [ ] 抖音那条能正常播放（竖屏）
- [ ] 计划页：能选部位、能加动作、点圆点有烟花
- [ ] **手机打开网址**，确认排版不乱
- [ ] 刷新页面，计划页的进度还在（说明本地存储正常）

---

## 常见问题

### Q：网址太长不好记？
免费子域名可以先改短（Netlify 改 site name、Cloudflare 改项目名）。
想更正式就买个域名（约 60–80 元/年），在平台里绑定即可。

### Q：更新了但网页没变？
1. 先按 `Ctrl + F5` 强制刷新（浏览器缓存）
2. 去平台看部署记录，确认是"成功"状态
3. 确认改的文件真的 Commit 了

### Q：手机能访问吗？
能。网址是公开的，任何设备都能打开。计划页的数据存在**每台设备自己的浏览器里**，手机和电脑的进度不互通。

### Q：别人能看到我的录入工具吗？
能，但里面只是生成代码，改不了你的文件，没有实际风险。真要藏起来可以在 Cloudflare 里加访问限制（对非程序员略复杂，先不用管）。

### Q：免费额度够吗？
这个网站很小（几百 KB），免费额度远远够用。除非以后月访问量上几十万，否则不用花钱。

### Q：会被搜索引擎搜到吗？
暂时不会。现在内容都是浏览器端渲染的，搜索引擎抓不到。
想解决要升级成静态生成（Astro），以后再说。

---

## 我的建议

1. **今天**：做方案 A，先拿到网址，手机打开试试
2. **这周**：做方案 B，以后更新就方便了
3. **之后**：继续往 `exercises.js` 和 `videos.js` 加内容
4. **动作库到 100 条后**：再考虑 SEO 改造

---

## 上线前可以让我帮忙的

- 加 favicon（浏览器标签图标）
- 加 robots.txt / sitemap
- 加分享预览标签（微信/群里分享链接时显示标题和缩略图）

需要就说一声。
