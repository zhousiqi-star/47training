---
name: 947fitness
description: 健身房现场的实心器械面板 —— 哑光实体面 + 一支电光青，没有玻璃、没有发光
colors:
  electric-teal: "#35E8C9"
  electric-teal-deep: "#22B8A0"
  electric-teal-bright: "#7CF3DE"
  on-accent: "#04211C"
  panel-black: "#0B0F11"
  panel: "linear-gradient(180deg,#1E272B,#151C1F)"
  panel-2: "linear-gradient(180deg,#283338,#1D2629)"
  panel-3: "linear-gradient(180deg,#161D20,#222B2F)"
  groove: "linear-gradient(180deg,#070B0D,#101619)"
  key-side-ink: "#05080A"
  slot: "#111719"
  slot-2: "#182024"
  edge-soft: "#212A2E"
  edge-strong: "#3A474D"
  edge-top: "#36434A"
  ice-white: "#EAF6F7"
  steel-mist: "#A9BDBF"
  steel-dim: "#8B9FA2"
  steel-faint: "#7B9093"
  alert-coral: "#FF7A6B"
  warn-amber: "#FFD166"
  shadow-ink: "rgba(0,0,0,0.82)"
  bevel-light: "rgba(255,255,255,0.4)"
  inset-ink: "rgba(0,0,0,0.55)"
  press-ink: "rgba(0,0,0,0.5)"
  glyph-ink: "rgba(0,0,0,0.55)"
  code-text: "#A8D8CE"
typography:
  display:
    fontFamily: "\"Cascadia Mono\", \"SF Mono\", ui-monospace, Consolas, monospace"
    fontSize: "clamp(46px, 13vw, 66px)"
    fontWeight: 600
    lineHeight: 0.9
    letterSpacing: "-0.045em"
  display-sub:
    fontFamily: "\"Cascadia Mono\", \"SF Mono\", ui-monospace, Consolas, monospace"
    fontSize: "26px"
    fontWeight: 600
    lineHeight: 1
  headline:
    fontFamily: "\"Segoe UI Variable Display\", \"Segoe UI\", system-ui, \"PingFang SC\", \"HarmonyOS Sans SC\", \"MiSans\", \"Microsoft YaHei UI\", sans-serif"
    fontSize: "22px"
    fontWeight: 650
    lineHeight: 1.3
    letterSpacing: "-0.015em"
  title:
    fontFamily: "\"Segoe UI Variable Display\", \"Segoe UI\", system-ui, \"PingFang SC\", \"HarmonyOS Sans SC\", \"MiSans\", \"Microsoft YaHei UI\", sans-serif"
    fontSize: "17px"
    fontWeight: 650
    lineHeight: 1.4
    letterSpacing: "-0.012em"
  body:
    fontFamily: "\"Segoe UI Variable Display\", \"Segoe UI\", system-ui, \"PingFang SC\", \"HarmonyOS Sans SC\", \"MiSans\", \"Microsoft YaHei UI\", sans-serif"
    fontSize: "15px"
    fontWeight: 400
    lineHeight: 1.6
  mini:
    fontFamily: "\"Segoe UI Variable Display\", \"Segoe UI\", system-ui, \"PingFang SC\", \"HarmonyOS Sans SC\", \"MiSans\", \"Microsoft YaHei UI\", sans-serif"
    fontSize: "13px"
    fontWeight: 600
    lineHeight: 1.5
  micro:
    fontFamily: "\"Cascadia Mono\", \"SF Mono\", ui-monospace, Consolas, monospace"
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.65
  label:
    fontFamily: "\"Segoe UI Variable Display\", \"Segoe UI\", system-ui, \"PingFang SC\", \"HarmonyOS Sans SC\", \"MiSans\", \"Microsoft YaHei UI\", sans-serif"
    fontSize: "11px"
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: "0.16em"
rounded:
  xxs: "4px"
  xs: "8px"
  sm: "12px"
  md: "16px"
  lg: "22px"
  xl: "28px"
  pill: "999px"
spacing:
  xxs: "4px"
  xs: "8px"
  sm: "12px"
  md: "16px"
  lg: "22px"
  xl: "32px"
  section: "20px"
components:
  button-primary:
    backgroundColor: "{colors.electric-teal}"
    textColor: "{colors.on-accent}"
    typography: "{typography.body}"
    rounded: "{rounded.md}"
    padding: "0 18px"
    height: "52px"
  button-quiet:
    backgroundColor: "{colors.panel}"
    textColor: "{colors.steel-dim}"
    typography: "{typography.label}"
    rounded: "{rounded.md}"
    padding: "0 18px"
    height: "52px"
  chip:
    backgroundColor: "{colors.panel}"
    textColor: "{colors.steel-mist}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
    padding: "6px 13px"
  plan-card:
    backgroundColor: "{colors.panel}"
    textColor: "{colors.ice-white}"
    typography: "{typography.title}"
    rounded: "{rounded.lg}"
    padding: "14px 16px 16px"
  focus-tile:
    backgroundColor: "{colors.panel}"
    textColor: "{colors.steel-mist}"
    typography: "{typography.label}"
    rounded: "{rounded.md}"
    padding: "10px 11px 30px"
  nav-pill-active:
    backgroundColor: "{colors.electric-teal}"
    textColor: "{colors.on-accent}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
    padding: "7px 15px"
  set-dot:
    backgroundColor: "{colors.panel}"
    rounded: "{rounded.pill}"
    size: "20px"
  set-dot-lit:
    backgroundColor: "{colors.electric-teal}"
    rounded: "{rounded.pill}"
    size: "20px"
  input-field:
    backgroundColor: "{colors.panel}"
    textColor: "{colors.ice-white}"
    typography: "{typography.body}"
    rounded: "{rounded.sm}"
    padding: "11px 13px"
---

# Design System: 947fitness

## Overview

**Creative North Star: "The Machine Panel"（实心器械面板）**

这个系统把自己当作健身房里一块**实心哑光的器械面板**：台面上嵌着几块比台面更亮的实体板，一支电光青只负责回答一个问题——**现在该做什么**。它不做说服，不做装饰，**不用任何半透明、模糊或发光**；层级完全由面与面之间的明度差、以及倒角的受光边表达。这来自产品最硬的那个场景：健身房顶灯很强，屏幕在阴影里，一只手拿着手机，注意力只剩几秒——**哑光的实心面在强光下不反光、不发灰，比玻璃好读**。

**明确的视觉反参考（两版）：**第一版是一张预印格子的「纸带记录仪」——浅色纸面、红橙格线、墨点轨迹，用户判词「我不要纸条风」。第二版是半透明玻璃模块 + 彩色光晕，用户判词「就不要玻璃感，要实打实的质感」。玻璃版里被否掉的三件事写在这里，防止回潮：**半透明面、`backdrop-filter`、彩色发光**。三者现在全站为零。

**Key Characteristics:**
- 台面近黑（`#0B0F11`），实体面是几档递增的实心灰（`#171E21` → `#1F282C` → `#212A2E`），凹槽反而更深（`#0D1214`）
- **零 `backdrop-filter`、零半透明面、零彩色发光**——层级只由明度差 + 倒角表达
- **只有一处大面积强调色**：首页顶部的读数表头（标题 + 日期 + 超大读数 + 进度 + 还差几组）是一整块实心青，占屏约 15%。除此之外青色一律小面积。
- 等宽数字承担所有读数，组数是页面上最大的东西
- 按下去必须有手感：位移 + 内侧阴影，过渡 70ms
- 手机优先：390px 是设计基准，桌面是把同一套语言摊开，不是另一套设计

## Colors

克制的中性底 + 单一强调，明暗由物理场景决定（顶灯强、屏幕在阴影里），因此在深底上做高对比读数，而不是在浅底上做低对比装饰。

### Primary
- **Electric Teal 电光青** (`#35E8C9`): 全站唯一的强调色。它只出现在五个地方：主操作按钮、进度填充、点亮的组数灯、当前读数与选中态、947 徽章。它出现的位置就是用户此刻该看的位置。
- **Electric Teal Deep** (`#22B8A0`): 进度条渐变的起点，给填充一个方向感而不是一块平色。
- **Electric Teal Bright** (`#7CF3DE`): 选中态里比主色再亮一档的文字，用在 `--accent` 压不住的地方（如选中格的个数）。
- **On Accent** (`#04211C`): 压在青色上的文字色。对比度约 11:1，青色块上永远用这个，不用白色。
- **On Accent 2** (`rgba(4,33,28,0.72)`): 青色面上的次级文字（日期、单位、说明）。对比 5.3:1。
- **On Accent 3** (`rgba(4,33,28,0.52)`): 青色面上的三级文字与装饰。
- **On Accent Track** (`rgba(4,33,28,0.24)`): 青色面上的凹槽（进度轨底、磁贴边框）。

### Neutral
- **Panel Black 台面** (`#0B0F11`): 页面底色。**不是纯黑**，带青色调，比实体面暗一档，这就是整个层级的基准线。
- **Panel 实体面** (`linear-gradient(180deg,#1E272B,#151C1F)`): 第一层实体面——动作卡、历史页的日清单、销轨行。**它不是平色，是"上亮下暗"的不透明斜面**，这是立体感的第一来源。
- **Panel 2 抬起的面** (`linear-gradient(180deg,#283338,#1D2629)`): hover、下拉项、聚焦后的输入框，以及**做满的动作卡**——比普通卡亮一档就是"抬起来"，不用第二个色相去表达"完成"。
- **Panel 3 输入面** (`linear-gradient(180deg,#161D20,#222B2F)`): 输入框与文本域的静止底，也是部位磁贴里 emoji 方块的底。**它反着来（上暗下亮）**，因为输入框是凹的。
- **Groove 凹槽** (`linear-gradient(180deg,#070B0D,#101619)`): 比台面**更深**——未点亮的组数灯、历史页的铭牌与销轨槽、进度轨。同样**上暗下亮**，凹进去的东西受光更少。
- **Edge Soft 常规边** (`#212A2E`): 实体面的 1px 边。它在明度上几乎和实体面同色，只负责勾出轮廓，不制造"浮起来"的错觉。
- **Edge Strong 强调边** (`#3A474D`): 空槽位的虚线边界。
- **Edge Top 倒角受光边** (`#36434A`): 实体面上沿那 1px 更亮的边——**整个系统里唯一表达"这是一块实体"的东西**。物理世界里光从上面来，所以只有上沿亮。
- **Ice White 冰白** (`#EAF6F7`): 主要文字与动作名。也带青调，和底同色相。
- **Steel Mist 钢雾** (`#A9BDBF`): 次级文字、副标题、未点亮的读数。对比 7.9:1。
- **Steel Dim** (`#8B9FA2`): 三级文字、标签、个数。对比 6.1:1。
- **Steel Faint** (`#7B9093`): 最低一级（序号、元信息、页脚）。**这是下限**，5.0:1，不允许再暗。
- **Press Ink** (`rgba(0,0,0,0.5)`): 按下去时内侧的影。让按钮看起来"陷进去了"。
- **Key Side Ink** (`#05080A`): 实体键与卡片的**侧面**。它比台面还深，所以键底下的那道实心偏移能读成"厚度"而不是"影子"。
- **Slot / Slot 2** (`#111719` / `#182024`): **空槽位的面**——还没填动作的那个格。它比实体面 `--panel` 更暗，因为它是一块**无边框按钮**：没有描边、没有厚度，只靠这块暗面和 ＋ 图标说明"这里可以放东西"。hover 换成 Slot 2。
- **Alert Coral** (`#FF7A6B`): 唯一的破坏性信号（清空今天的记录、删除动作、字段错误）。除它以外全站没有第二个暖色。
- **Warn Amber** (`#FFD166`): 就地警告文字。它是**状态色**，不和任何分类色混用。
- **Shadow Ink** (`rgba(0,0,0,0.82)`): 所有**向外**的抬升投影。中性、无彩，压制水花。
- **Inset Ink** (`rgba(0,0,0,0.55)`): 凹槽内部的影（未点亮的灯、进度轨、输入框底）。它让凹进去的东西真的凹进去。
- **Bevel Light** (`rgba(255,255,255,0.4)`): 有色实心块上沿的倒角受光。青色按钮、点亮的灯、选中的部位胶囊都用它——底下有颜色，所以受光必须用白而不是灰。
- **Glyph Ink** (`rgba(0,0,0,0.55)`): emoji 落在照片上的投影。只在照片类背景上使用。
- **Code Text** (`#A8D8CE`): 代码块的文字色。代码块底用的是 `--groove`。全站唯一的"机器输出"配色，故意和阅读区拉开。

### Named Rules
**The One Voice Rule.** 强调色只有一支。屏幕上任何一处出现电光青，都必须能回答「这是我此刻该操作或该注意的地方吗」。回答不了的地方，用中性色。

**The One Field Rule.** 全站只允许**一处**大面积强调色，就是首页顶部的读数表头（约 15% 占屏）。它是一整块实心青，深色字压在上面。其余所有地方——选中的磁贴、点亮的灯、CTA、进度填充——都是小面积。

> **推论（训练历史页就是这么来的）**：既然那 15% 只有一处，历史页就**没有**任何青色大块。它用**深度**说话——铭牌是一块挖出来的凹槽，数字刻在里面；青只做区间胶囊、"今天"那道开口弧的指针、以及销轨上的销子。**首页用面积说青，历史页用深度说青。**

> **这个 15% 是怎么来的（重要，别再来回改）。** 用户最初的直觉是"像参考图那样一大块颜色"，参考2 的实测是 31.5%。但把三张参考图放在同一口径下量，结果互相矛盾：**参考1 只有 3.8%、参考3 只有 0.6%**，而参考2 的 31.5% 里绝大部分是**作品展示板的背景色块**（一块青柠底板托着三台手机），不是 App 界面。也就是说"大面积强调色"只来自一张图，而且被展示板的水分抬高了。用户看过这组数字后把目标定在 **15%**。实测：手机 17.8% / 桌面 14.8%。**不要再往 28% 推。**

**The Floor Rule.** 灰阶的对比度必须按**文字实际坐着的面**算，不是按台面算——页面上没有文字坐在裸台面上，它们坐在实体面 `--panel` (`#171E21`) 上。三级灰的实测值是 7.9 / 6.1 / 5.0:1，最低一级 `--ink-dim` (`#7B9093`) 就是地板。任何新加的灰都必须落在这条线以上，正文级还要更高。

**The One Accent, One Atmosphere Rule.** 环境光只有青色一支，来源也只有一处（顶部顶灯）。不许为了"层次"往背景里加第二个色相——分类色相是数据，不是气氛。

## Typography

**Display Font:** 系统无衬线栈（`Segoe UI Variable Display` → `PingFang SC` → `HarmonyOS Sans SC` → `Microsoft YaHei UI`）
**Body Font:** 同一套系统无衬线栈
**Label/Mono Font:** 等宽栈（`Cascadia Mono` → `SF Mono` → `Consolas`）

**Character:** 标题用系统无衬线的 650 字重、负字距，克制而结实；所有数字改用等宽。这不是审美偏好，是硬约束——**中国大陆必须能直接打开，所以一个外部字体都不许下载**，系统栈是唯一正确答案。数字用等宽则是功能性的：组数在跳动时不能左右抖。

### Hierarchy
刻度一共八级，全部落在 `--s-*` 变量上，任何地方都不许写裸 px 字号。

- **Display** (600, `clamp(46px, 13vw, 66px)`, 行高 0.9, 字距 -0.045em): 唯一的巨型元素，只给「今天完成了多少组」。全站没有任何别的东西可以和它比大小。
- **Display Sub** (600, 26px, 等宽): 读数里的分母与单位（`/ 16`、`组`）。它必须明显小于 Display，否则读数就不是一个数了。
- **Headline** (650, 22px, -0.015em): 页面标题（今天的训练 / 训练历史）。
- **Title** (650, 17px, -0.012em): 动作名、区块标题、历史页的月份头与那天清单的日期；也是圆形图标按钮里的字形尺寸上限。
- **Body** (400, 15px, 1.6): 说明文字与输入值。段落宽上限约 68ch。
- **Mini** (600, 13px, 1.5): 介于 body 与 label 之间的次级信息：chip、脚注、按钮文字、卡片里的组数读数。
- **Micro** (400, 12px, 1.65, 等宽): 代码块与行内代码。全站唯一的"机器输出"字号。
- **Label** (600, 11px, 字距 .16em, 全大写): 标签类文字（完成组数、范围、日期、字段名）。
- **Glyph** (`--g-sm 14 / --g-md 18 / --g-lg 34 / --g-xl 46`): **图标与 emoji 的字形尺寸不属于排版刻度**，单列一组。它们量的是图形的视觉重量，不是阅读节奏，所以不参与上面八级。

### Named Rules
**The Readout Rule.** 任何"数量"都必须是等宽数字并开启 `tabular-nums`。数量在页面上降级成小字是禁止的——数量就是这个界面的主角。

**The Two-Stacks Rule.** 全世界只有两套字：无衬线（人话）和等宽（数字与代码）。第三套字体不允许引入，包括为了"高级感"引入的衬线。

**The Glyph-Is-Not-Type Rule.** 图标和 emoji 只准取 `--g-*` 四个值。它们绝不允许混进 `--s-*` 排版刻度，反过来也一样。机械检测器会把 `--g-*` 报成"偏离排版刻度"——这是已知且有意的，不要为了消掉告警去改它。

## Layout

单一内容列，`--wrap: 940px`，两侧留白 20px。手机是设计基准：390px 下所有东西单列，动作卡一列、部位选择收成一条可横滑的吸附带（`scroll-snap-type: x proximity`，每格 104px）——**在健身房现场不该为了选部位先滚三屏**。

760px 以上动作卡变两列；历史页在 760px 以上把铭牌与区间并排、销轨铺成两列（环历保持单列，宽度上限 540px）。桌面不重新设计，只是把同一套语言摊开。

垂直节奏：区块之间 20px，卡片之间 12px，卡内 14–16px。手机端主操作栏 `position: sticky; bottom: 0`——**它不是一个层，是台面延伸到屏幕底边的那一条**（无边框、无圆角、面无色，见 The No-Layer Rule），单手拇指可及。

间距刻度：4 / 8 / 12 / 16 / 22 / 32。

## Elevation & Depth

**立体感来自材料的成形，不来自光。**深度由四件事表达，没有第五件：

1. **明度阶梯**——台面 `#0B0F11` < 实体面 < 抬起的面。亮的一层就是靠近观察者的一层。
2. **斜面**——面本身不是平色，是"上亮下暗"的不透明渐变。凸起的东西上沿朝向光源；凹的（输入框、凹槽）反过来，上暗下亮。
3. **倒角受光边**——面上沿那 1px 的亮边。物理世界的光从上面来，所以只有上沿亮；这条边是"这是一块实体"的直接证据。
4. **实体键的四个部件**（只给可点控件）——斜面 + 上沿受光 `--key-top` + **下沿厚度** `--key-bottom` + 贴地短影 `--key-ground`。第三条最要紧：**下沿那道 2px 的暗边就是键的侧面**，有了它键才站起来。按下去时这一层整个换成"陷进去"的样子。

投影只做一件事：把浮动的整块面（选择面板、庆祝横幅、移动端浮动操作栏）从内容里拉出来，一律中性黑、短程、低不透明度。**全站没有一个彩色发光**：点亮的组数灯是实心青加一道倒角，做满的卡片是**亮一档的中性面 + 青色描边**，读数板做满由文案和满格进度条表达。状态全部由"实心色块 + 文字/数字"表达，绝不靠光。

### Shadow Vocabulary
- **bevel** (`inset 0 1px 0 #36434A`): 面上沿的受光边。
- **bevel-light** (`inset 0 1px 0 rgba(255,255,255,.4)`): 有色实心块（青色按钮、点亮的灯、选中胶囊）上沿的倒角受光。用白而不是灰，因为底下有颜色。
- **key-top** (`inset 0 1px 0 rgba(255,255,255,.12)`): 实体键的上沿受光。青色键上用更亮的 `--key-top-on`。
- **key-bottom** (`inset 0 -2px 0 rgba(0,0,0,.5)`): **实体键的下沿厚度**。这是"立体"最关键的一条——它画出键的侧面。
- **key-ground** (`0 3px 7px -3px rgba(0,0,0,.85)`): 实体键贴地的那一层短影，表示键离底面还有一点点距离。
- **card-top / card-bottom / card-side / card-ground** (`inset 0 1px 0 #36434A` / `inset 0 -8px 0 rgba(0,0,0,.62)` / `0 12px 0 -1px #05080A` / `0 18px 16px -14px rgba(0,0,0,.95)`): **动作卡片专用的加厚版**。卡片面积大，小键那套厚度放在它身上会显得薄，所以下沿加倍、侧面用实心偏移、贴地影更远。hover 时侧面上抬到 15px、"抬起来"的幅度也更大。
- **recess** (`inset 0 2px 4px rgba(0,0,0,.55)`): 凹槽。未点亮的灯、轨道、输入框底。
- **press** (`inset 0 2px 6px rgba(0,0,0,.5)`): 按下去时内侧的影。
- **float-md** (`0 6px 16px -10px rgba(0,0,0,.82)`): 真正浮在内容之上的东西：庆祝横幅。**上限就是 16px 模糊**——再宽就会被机械检测器判成「细边 + 大范围柔影」那个 AI 指纹。选择面板不投影：它坐在 84% 的遮罩上，遮罩本身就是分离。**底部动作栏不投影也不给面**——它不是一个层，见 The No-Layer Rule。

### Named Rules
**The Key Rule.** 每一个**可点**的控件都必须是一个实体键：斜面面 + 上沿受光 + 下沿厚度 + 贴地短影，四件缺一不可；按下去时整层换成"陷进去"。厚度分三档：**小键**（chip、步进器、图标钮，`--key-*`）→ **动作卡片**（`--card-*`，面积大所以下沿 6px、侧面 8px，**全站最厚的东西，除读数表头外**）→ **浮层**（选择面板、横幅，走 `float-md`）。

**The No-Layer Rule.** 底部动作栏**不是一个层**。卡片是"放在台面上的板"，动作栏**不是板，是台面本身延伸到屏幕底边的那一条**：没有边框、没有圆角、没有自己的面色（背景就是 `--ground`）、贴到屏幕底边（`bottom: 0`）、两侧撑到屏幕边。栏里的次要动作（`清空今天的记录`）做成**纯文字**，不给它第二个色块——否则栏里立刻又多出一层。上沿只接一道 18px 的渐隐（台面色变透明），那是过渡，不是第二块面。

> **这条改了三轮才对，过程记下来免得回潮：**
> 1. 第一版只去掉 `box-shadow` → 还是鼓的。因为 `--panel` 本身已经是"上亮下暗"的斜面渐变，**影去掉了面没平**。
> 2. 第二版把面换成平色 `--strip` → 平了，但**还是一块板**浮在内容上。
> 3. 第三版才看清：**问题不在平不平，在于它是不是一块板。** 于是直接取消它的面、并进台面——也就没有再需要 `--strip` 这个 token。
>
> 桌面端 `.plan-foot` 本来就没有面，所以这条只在手机断点里执行。

**不可点的空槽位不给厚度**——它是"洞"，不是"键"，这样填了动作的卡和没填的卡一眼分得开。

**The Contact-Shadow Rule.** 贴在台面上的**面板**不投外阴影——抬升由明度阶梯 + 倒角表达。只有真正浮在内容之上的东西（选择面板、庆祝横幅、浮动操作栏）才有投影，而且必须是短程（≤16px 模糊 + 负扩散）硬影。实体键的 `--key-ground` 是例外里最小的那一档：它是"厚度"不是"浮起来"。**「1px 细边 + 50px 大范围柔影」是 AI 生成界面的典型指纹，明令禁止。**
**The No-Light Rule.** 全站不允许出现彩色发光、彩色投影或零偏移的彩色晕。状态用**实心色块**表达，不用光。这是用户否掉玻璃版时明确要求的一条，也是这一版最容易被回潮破坏的一条。

**The Solid Rule.** 不允许有**半透明**面，不允许有 `backdrop-filter`。一块面要么是不透明的色/渐变，要么是凹槽，没有第三种。要分层就改明度，不要改透明度。**注意"实心"指的是不透明，不是"平色"**——面本身允许带一道不透明的斜面渐变（第 2 条机制）。

**The Bevel Rule.** 每一块实体面的上沿必须有 1px `--edge-top`。少了它，这块面立刻变回一个贴纸。

**The Press Rule.** 每一个可点的东西按下去都必须有反馈：位移 1–2px + 内侧影，过渡 70ms。松手走回元素自己的 180ms，于是"进得快、回得稳"。没有按压态的可点元素等于没做完。

## Shapes

圆角分三档，内紧外松：小控件 12–16px（chip、输入框、磁贴），卡片与面板 22px，大容器 28px，胶囊一律 `999px`。图标按钮是正圆，不用方角。

边界极轻：实体面的边是 `--edge-soft`，在明度上几乎和面同色，只负责勾出轮廓，不制造"浮起来"的错觉。状态改变时边才换成实心青。

**空槽位没有边。** 它是无边框按钮（面 `--slot`、无描边、无厚度、整块可点），和填了动作的厚卡片靠三件事区分：面更暗、没有厚度、没有边。

> **踩坑：`border: 0` 不等于"无边框"。** 空槽位按钮原来和部位磁贴共用一条规则，拿到了 `--key-top`，也就是 `inset 0 1px 0 rgba(255,255,255,.12)`。按钮又撑满卡片宽度，于是顶部横着画出一条 1px 亮线——**看起来完全就是一道边框**。它还顺带拿到了贴地影。
> 教训：**任何在元素边缘画出线的 inset 影，都会读成边框。** 要"无边框"就必须同时清掉 `border`、上沿受光、下沿厚度、贴地影、以及任何边缘描边。这条和 The No-Layer Rule 是同一个道理。

**虚线现在全站为零**：唯一一处（录入工具的封面拖拽区）随视频页一起删掉了。再出现虚线，先问它是不是在表达"把东西拖到这里"。

**凹槽与面必须成对使用**：一个面（`--panel`）配一道上沿倒角（`--edge-top`）才是实体；一个槽（`--groove`）配一层内侧影（`inset-ink`）才是凹的。把凹槽色当背景用、却不加内侧影，看起来就只是"一块更黑的东西"。

唯一的几何例外是部位磁贴底部的柱——它是数据，不是装饰；而它是**中性灰**的，不引入第二个颜色。

### Named Rules
**The Height-Is-Data Rule.** 六个部位磁贴共用**一条轴**：柱高 = 该部位动作数 ÷ 六个部位里的最大值，不做装饰性的下限压缩（早期版本的 `26% + ratio × 62%` 会把 3 个和 4 个压成 5px 的差别，那是在浪费用户的信任）。柱子只活在格子底下专门留出的 30px 轨道里，永远不许压到文字。**柱子是中性灰（`--ink-3`，38% 不透明度），不带颜色**——数据编码靠高度，不靠色相。

**The Off-Axis Rule.** 「全部动作」不是部位，不在这张图的轴上，所以它**没有柱子**。给一个不同量纲的东西画同一条轴上的柱子，等于在图里撒谎。

**The Small-Area Rule.** 分类色相（`--tone-*`）**已经从系统里整组退场**（`search-core.js` 里那个 `tone` 字段是历史遗留，不再被任何样式读取）。它原来只剩两个家——视频封面替身底和录入工具取色器——那两个页面已经删除，所以**全站现在真的只剩一支电光青加中性灰**。部位靠 emoji 和名字区分就够了。

> 这条是从"小面积数据编码"一路收紧过来的：最初六个部位各自带色柱和 emoji 色底，用户看过后要求全去掉，原话「把小卡片里的其他颜色都不要」。改完实测：整页只剩一个色相（青色 59054 px），其余加起来 1165 px（1.9%），而且那些是 **emoji 字形自带的颜色**，改不掉。**不要再把色相加回磁贴。**

## Components

### Buttons
- **Shape:** 方角柔化（16px 圆角），高 52px，手机上撑满宽度
- **Primary:** 电光青实心 + `#04211C` 文字（`添加动作`）。投影必须是中性黑，不许发光。
- **Hover / Focus:** hover 提亮 6% 并上移 1px；按下回弹 1px；键盘焦点是 2px 电光青描边、偏移 2px。
- **Quiet:** 实体面 + `--ink-dim` 文字（`清空今天的记录`）。hover 时才转成 Alert Coral，破坏性动作在静止时不喊叫。

### Chips
- **Style:** 实体面、`999px`、11px 全大写标签字、`--ink-2` 文字
- **State:** 选中的胶囊用电光青实心 + 深色字（分段导航），或者青色调底 + 青色文字（范围切换）。颜色永远不是唯一信号：选中的同时还换文字色和底色。

### Cards / Containers
- **Corner Style:** 22px（动作卡、历史页展开的那一天），28px（读数板、选择面板、历史页的铭牌）
- **Background:** `--panel` 斜面面；**空槽位是一块无边框的暗面**（`--slot`，没有描边、没有厚度，整块可点——它是个无边框按钮，不是"洞"）；做满的卡片换成 `--panel-2` 更亮的斜面 + `--accent` 描边——"完成"由抬升、青色描边、青色的组数灯和读数三条一起说，**不用第二个色相**
- **Shadow Strategy:** `--card-top / --card-bottom / --card-side / --card-ground` 四件套。**动作卡片是全站最厚的东西**（除读数表头）：下沿 8px 厚度 + 12px 实心侧面 + 18px 贴地影。hover 时整张上抬 2px、侧面到 15px
- **Border:** 1px `--edge-soft`；做满时换成青色描边
- **Internal Padding:** 14–16px；面板 20px

### Inputs / Fields
- **Style:** `--panel-3` 实心底 + 1px `--edge-soft` 描边 + 12px 圆角，高 44–46px，**内侧影**（输入框是凹的，不是浮的）
- **Focus:** 描边转青 + 一层 `0 0 0 3px` 的青色调外圈。**不用发光**
- **Error:** `--alert-coral` 文字色，错误信息就地显示在字段下方，绝不使用 `alert()`

### Navigation
实体分段控件：容器是 `--panel` 实心胶囊（带内凹），当前页是电光青实心（带倒角，像一块压进去的键）。手机上收进 56px 高的实心顶栏，不做底部 Tab（全站两个页面：今日训练 / 训练历史，顶部已经够用，底部再放一条导航是负担）。

### Press（每个可点元素都要有）
按下 = 位移 + 内侧影，`transition-duration: .07s`；松手走回元素自己的 180ms，于是"进得快、回得稳"。分四档行程：

| 对象 | 按下时 |
|---|---|
| 主操作（添加动作） | `translateY(2px) scale(.995)` + `inset 0 3px 9px` + 露出底部的受光边 |
| 实体面小按钮（chip / 迷你按钮 / 模式切换 / 圆形图标钮） | `translateY(1px) scale(.95)` + `inset 0 2px 6px` |
| 步进器 ± | `translateY(1px) scale(.9)` + `inset 0 2px 5px` |
| 组数灯 | `scale(.84)` + `inset 0 3px 6px`（像真的按到一颗灯上） |
| 整块可点的面（磁贴 / 空卡片 / 列表行） | `translateY(1px) scale(.994)` + `inset 0 2px 10px` |
| 链接类（导航 / 在新窗口打开 / 返回） | 只做 `translateY(1px)`，不加影 |

组数灯被点亮时另有一次 `scale(.55) → 1.22 → 1` 的回弹（`dot-pop`，340ms）——那是**结果**的反馈，和按下去的**触感**反馈是两件事，两层都要有。

### Readout Board（签名组件）
首页顶部的**青色表头**，全站唯一一块大面积实心青（约 15% 占屏）。它包含：页面标题「今天的训练」、日期、超大等宽读数 `6 / 17 组`、8px 凹槽进度轨、右侧回答产品故事那句话的「还差 11 组」，以及「1/4 个动作已完成」。

它是一个**连成一体的块**：`.plan-head` 拿上圆角、`.meter` 拿下圆角，中间没有缝（`margin` 为 0）。深色字（`--on-accent` 系列）压在实心青上，读数板内部**没有边框、没有投影**——青本身就是它的形状。

三件事各司其职：读数说"做了多少"，轨道说"占比多少"，右侧说"还差多少"。**不要用百分比去填右边那个位置**——比例已经由前两者表达两遍了，第三次是噪音，而"还差几组"才是用户点开页面想知道的东西。进度填充用 `--on-accent`（青面上的深色），空轨用 `--on-accent-track`。

### Set Dot（签名组件）
这颗灯是整个产品的记忆点。**未点亮是一颗凹进去的孔**：`--groove` (`#0D1214`) 实心底 + `inset 0 2px 4px` 的内侧影，比台面更深，因为没通电的东西不发光、只吃影子。**点亮是一颗实心的灯**：电光青实心 + 上沿倒角 + 下沿一道暗边，没有色晕——它亮，是因为它是实心的青，不是因为它发光。点下去时先有一次 `scale(.84)` 的按压，再走 `scale(.55) → 1.22 → 1` 的回弹（`dot-pop`，340ms）。**同一时刻，顶部的读数与进度条一起前进一格**——灯亮、数字跳、条长，三件事必须同帧发生，这是"记录"这个动作的全部回报。

**尺寸的诚实说明：**一组最多 12 颗，12 × 44px 在手机上物理上排不下，所以组数灯取 30px；**圆点占满整行、允许换行，步进器自动让到下一行**——12 组实测是 2 行圆点 + 1 行步进器，不是 3 行。真正孤立的按钮（删除 32px、关闭 40px）用 `::after` 把可点区域撑到 44px，视觉不变。

### Ring（训练历史页的签名组件）
一天一枚环，它只回答一件事：**这一天有没有被封存过**。
- **闭合的圆** = 已封存（那天在首页按过「封存今天的清单」）：全圈实心青，没有指针
- **开口的弧 + 末端指针** = 有记录但还没封存（含"今天"）：3/4 圈，起笔在 12 点，顺时针，末端一道 6×2 的青色指针
- **空槽** = 那天没练：只有一枚 30px 的凹槽环（`--groove` 实心底 + `inset 0 2px 4px` 内侧影），没有弧
- **还没到的日子** = 不是孔，是还没加工的一块面（`--panel` + 上沿倒角，55% 不透明度）

**两条数据编码，都沿用首页的纪律**：环的粗细 = 那天完成组数 ÷ 本区间最大值（共用一条轴，不做装饰性下限）；
环下的小字写同一个数字。**颜色、粗细都不是唯一信号——数字必须写出来。** 环心的数字是那天的日期（这是日历，先让人找到"哪天"；
今天那天用青色）。

### Ledger（历史页的铭牌）
区间读数刻在一块**挖出来的凹槽**里（`--groove` + `inset 0 2px 6px`，下沿一道极淡的受光）：
训练天数 / 完成组数 / 最重的一组，三个等宽数字用 `--s-display-sub`（26px）压在上面，下面一句中性对比。
**它是首页读数表头的反面**：表头用面积说青，铭牌用深度说。铭牌上的数字不许长到 `--s-display`——全站最大的数字只有一个。

### Rail（销轨：重量的语言）
一个动作一行水平凹槽（`--groove` + 内侧影），**销子（实心青，6px）= 最近一次**，**刻痕（中性灰，2px）= 最重的一次**。
所有行共用**一条 kg 轴**（轴长 = 区间内最大值），所以动作之间可以直接比；两个等宽数字写"最近"和"最重"。
没有销子的动作不画行——不许拿空槽假装数据。

### Quiet / Danger（删除类）
删掉记录是**破坏性动作，静止时不喊叫**：纯文字、`--ink-dim`，hover 才转成 `--danger` 并铺一层 `--danger-soft` 底；
按下只做 1px 位移，不给它第二个色块。它和首页底部「清空今天的记录」是同一条纪律（见 The Press Rule），
只是从"底部动作栏里的一条文字"搬到了页面上。可点高度仍是 44px。

两处落点：那天清单里的「删除这天的记录」，以及页面底部的「清空{当前区间}的记录（N 天）」——**按钮文案必须写清它要删几天**。

**破坏性动作永远过一道 `confirm`，而且确认文案里必须写出"会连带走什么"**：
删掉"今天"的时候，首页工作区里的动作卡也会一起清空——只删日志的话，用户回首页点一下圆点，
这一天会自己写回来（日志是 `save()` 自动写的）。这条是产品真相，不是实现细节。

### Snapshot Card（今天这张记录）
`card.js` 用 Canvas 手绘，零依赖（不引 `html2canvas` 这类外链——中国大陆可用是硬约束）。规格 1080×1620（2:3）。

- **它不是纸**：没有白底、没有格线、没有撕边、没有票据感（那正是被否掉的纸条风）。
  它是**读数板被拍下来的一张图**，材质与页面同一套：台面 `#0B0F11` + 顶部顶灯 + 极细噪点 + 实体面的上沿倒角。
  机械可验：全卡**纯白像素 = 0**（实测 0 / 抽样 20 万）。
- **一块青**：整张卡只有读数板那一块实心青（实测占卡面 32.0%）。One Field Rule 守的是"只有一处强调色"，
  不是那个 15%——15% 是页面口径，卡上没有别的 UI 要和它分这块面积。
- 数字一律等宽；卡上任何位置都不许把数量降级成小字（The Readout Rule 跟着离开页面）。
- **图里不写重量**：卡面只回答"练了什么、做了几组"。重量属于历史页的销轨和复制出去的纯文本；
  机械可验：同一份数据带重量与不带重量画两次，**校验和必须完全相同**（实测一致）。
- **不挤**：读数板与清单之间、清单与页脚之间各留 56px 台面。宁可留白，不许为了填满把中间挤死；
  动作多了自动分两栏、行高在 64–120 之间自适应，字号跟着行高走，不裁切。
- **可复现**：噪点用固定种子的伪随机，同一份数据永远画出同一张卡。
- **封存印**是页面上唯一一个青色调底的胶囊（`--accent-soft` 底 + `--accent` 文字）。
- 清单超过 6 个动作自动分两栏，行高自适应（下限 56px），字号跟着行高走——**不许裁切、不许缩到看不清**。
- 输出门槛：**没封存的一天不给输出**（见 PRODUCT.md 里记的工作方式）。

### Icons
全站图标是**一套自绘 SVG 掩膜**（14px，统一 1.75 描边，`mask-image` + `currentColor`）：✕ 删除/关闭、− ＋ 步进、‹ › 翻上/下一个月、＋ 添加动作。它们不是 Unicode 字形——参考图里的圆形按钮画的是图形。掩膜用 `currentColor`，所以图标颜色自动跟着 hover / 禁用状态走，且**不引入任何图标资源，也不下载任何东西**（中国大陆可访问是硬底线）。部位的 emoji 是内容，不走这套图标。

### Focus Tile（签名组件）
部位磁贴：28px 的 emoji 方块（**中性底** `--panel-3`）、名称、个数，底部 30px 数据轨里一条**中性灰**的柱。选中的磁贴整块变成电光青实心、文字转深色。它同时承担导航和数据可视化两件事：柱子读的是"这个部位有几个动作"，翻面读的是"今天练这个"。

**注意它不再是全站面积最大的强调色**——那块是首页的读数表头（约 15% 占屏）。磁贴只在被选中的那一格出现青色，约 1.6%。

> **判据留痕（给下一轮的构建者和机械检测器）：**历史页的销轨行（`.rail` 里嵌 `.rail-track`）是**有意保留的**：外层是"一个动作一条记录"的行，里面的槽是凹槽（配内侧影），两者是构造关系不是卡中卡。检测器如果把它报成 `nested-cards`，不要为了消告警去拆掉它。

## Do's and Don'ts

### Do:
- **Do** 把数量做成等宽数字并上 `tabular-nums`，让它成为页面上最大的东西。
- **Do** 用实心面表达层级：改明度（台面 → 实体面 → 抬起的面），不改透明度。
- **Do** 给每一块实体面的上沿一道 1px `--edge-top` 倒角；少了它，这块面立刻变回一张贴纸。
- **Do** 让每个可点的控件都是一个实体键：斜面面 + 上沿受光 + 下沿厚度 + 贴地短影。
- **Do** 让凹槽比台面更深，并配内侧影——凹进去的东西受光更少，而且斜面要反过来（上暗下亮）。
- **Do** 让每个可点的东西按下去有反馈：位移 + 内侧影，过渡 70ms。
- **Do** 让强调色在一处形成面积（首页读数表头），在其余所有地方保持小面积。
- **Do** 在青色表面上只用 `--on-accent` 那一组深色字，不要在青上压白色。
- **Do** 用三档圆角（12–16 / 22 / 28）表达内外层级，内紧外松。
- **Do** 让状态同时有颜色和文字/数字两重信号——颜色不能是唯一信号。
- **Do** 把彩色光留给状态（点亮的灯、进度、做满的卡），把中性黑投影留给抬升。
- **Do** 用系统字体栈：中国大陆必须能直接打开，一个外部资源都不许下载。
- **Do** 在 390px 宽度下先确认，再考虑桌面。
- **Do** 让读数板回答「还差几组」——那是产品故事里的那句话，不是又一个百分比。
- **Do** 让孤立按钮的可点区域到 44px（用 `::after` 撑开），视觉尺寸可以更小。
- **Do** 给每个键盘可到达的控件一个看得见的焦点环；关掉 outline 就必须补一个回来。

### Don't:
- **Don't** 引入第二个强调色，也**不要把任何色相加回页面本体**——分类色相已经整组退场，磁贴、卡片、标签、环、柱、状态一律只有青色加中性灰。
- **Don't** 用发光去装饰按钮或"提升质感"。**全站不允许任何彩色发光**——状态用实心色块表达。
- **Don't** 用半透明面、`backdrop-filter` 或渐变蒙版去"分层"。这一版是用户明确否掉玻璃版之后做的，回潮即违约。
- **Don't** 让可点控件跟不可点的面板长得一样——可点的一律要有下沿厚度和贴地影，不然用户分不出哪里能点。**唯一的例外是底部动作栏**，它刻意不成为一层。
- **Don't** 让部位色相跑到填充、文字色或状态表达里去。
- **Don't** 用第二个色相表达"完成"——做满的卡片是**亮一档的中性面 + 青色描边**，不是绿色。
- **Don't** 给数字换非等宽字体，或让数量降级成小字。
- **Don't** 把色柱放到文字背后——它只能活在磁贴底部那条 30px 的数据轨里。
- **Don't** 把不同量纲的东西画到同一条轴的柱状图里（「全部动作」没有柱子）。
- **Don't** 拿分类色相当状态色用（就地警告要 `--warn`）。
- **Don't** 把状态做成只有颜色一个信号——横滑带里半张卡片露在外面时，用户看不到边线。
- **Don't** 回到浅色纸面、格线、墨点那套语言，用户已经明确否掉了它。
