# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主用户是站主本人。** 一个有力量训练习惯的人，自己记录当天的训练。

用户明确说明（原话）："先自己用会尽量去推广"，并且明确拒绝个人品牌叙事——
"为什么要回答我是谁凭什么信我，我只需要一个用于记录的网站"。

→ 本产品**不需要说服层**：不做"关于我"、不做信任背书、不做转化路径。
  第一个使用者就是唯一确定的用户。

后续可能推广给同学/朋友，但这不是当前已确认的需求。[open: 多用户场景是否需要，未确认]

## Product Purpose

**把每天的训练记下来，并让"变化"在长期累积后能被看见。**

用户选定的定位是「时间胶囊」：
- 输入 = 每次训练的点选记录
- 价值 = 长期累积的历史、对比、里程碑
- 成功 = 自己持续记录，并且回头看时能确认自己变了

## Positioning

与训记 / Hevy / Keep 这类训练记录 App 的差别在于**"让变化可见"**，而不是记录本身。

记录功能是红海且免费；"把缓慢的身体变化显影出来"是用户选定的存在理由。

具体呈现机制现已落定：**一天 = 一枚环，"闭合 = 这一天被你确认过"**（详见 `history.html` 与 DESIGN.md 的 Ring 段）。
用户否掉了"完成度百分比"这条路线——原话：「我不需要回答是否完成，我只会在做完全部动作才输出一张今日的清单」。

## Operating Context

- **健身房现场**：单手拿手机、正在训练、注意力有限。[推断自"必须在手机上能用"，未逐条确认]
- **家庭桌面**：站主也会用电脑打开同一套页面。
- 单次打开时间很短，一天可能多次打开。
- **网络环境是中国大陆。**

## Capabilities and Constraints

### 已确认的功能范围（重设计必须完整保留）

- **今日训练记录**：选部位 → 从动作库选动作 → 点圆点记组数；每个动作可调总组数（1–12）
- **部位分类**：练胸 / 练背 / 练肩 / 练腿 / 练核心 + 全部动作（用户明确要求删掉「臀部」和「有氧训练」两组）；选定后收窄动作搜索范围
- **动作库**：56 个动作（胸 13 / 背 14 / 肩 10 / 腿 3 / 核心 7 / 手臂 9）。每条至少有名片字段：别名、肌群、器械、难度；部分条目还带步骤、常见错误、提示、禁忌（可选，没写也正常）
- **重量记录**：每个动作可选填一个 kg（只填数字，不填单位），随当天记录一起进日志
- **封存**：做完全部动作后按「封存今天的清单」，这一天在历史里才算闭上（同一天可再按一次更新时间戳）
- **训练历史**：月环历（闭合 = 已封存 / 开口 = 还没封存 / 空槽 = 没练）+ 点一天展开那天的清单 + 三档区间（本月 / 本年 / 全部）+ 重量销轨
- **删除记录**：历史页可以删单天，也可以按当前区间清空（本月 / 本年 / 全部）；
  删掉"今天"时会同步清空首页工作区（否则首页一点圆点就把这天写回来）
- **输出记录**：封存之后可以"取走"今天这张卡 —— 1080×1350 的竖版读数快照（保存图片 / 复制文字）；
  没封存的一天不给输出（封存是盖印，输出是取走那张卡）
- **完成反馈**：单个动作做满、整份训练做完、封存那一下，三级由强到弱的反馈
- **数据存储**：只存浏览器 localStorage；`fit-plan-v1` 是今天的工作区（按日重置），
  `fit-history-v1` 是只增不减的训练日志（每天一条，供历史页与月/年聚合使用）

### 硬约束（用户本轮勾选确认）

1. **必须在手机上能用**
2. **必须在中国大陆不挂 VPN 直接打开**

用户**没有**勾选"必须保持纯静态、无后端"，因此后端并非禁止项。[confirmed: not selected]

### 已知冲突

- 当前部署在 `*.workers.dev`，该域名在中国大陆被墙 → **与硬约束 2 直接冲突**（未解决）
- 长期累积：**已解决一半** —— 日志（`fit-history-v1`）不再按日重置，历史页能读它；
  但数据仍只在本机浏览器里，**清理浏览器数据或换设备就全没了**，导出/备份还没做
- 单机数据没有交叉校验：日期取自设备本地时钟，改系统时间会写进错误的日期（未解决）

## Brand Commitments

- 名称：**947fitness**
- 标识：左上角「947」青色徽章 + 「fitness」字标（合起来读 947fitness）
- 主色：**电光青 `#35E8C9`** —— 近黑台面（`#0B0F11`）上唯一的一支强调色
- 材质：**实心哑光**。没有半透明面、没有模糊、没有彩色发光；层级由明度差和倒角表达

> **决策留痕。** 早期版本的主色是橙色系（当时由用户要求）。此后经过两轮整体替换：先换成电光青玻璃材质，再按用户「就不要玻璃感，要实打实的质感」把玻璃层整层拆掉。**橙色作为品牌承诺已作废**，不要再作为回退选项提出——用户在本轮的原话是「橙色系不要」。
>
> 完整的视觉系统（token、命名规则、组件、禁区）见 **`DESIGN.md`**；单页面的方向契约见 `.impeccable/surfaces/`。

## Evidence on Hand

- 动作数据：`exercises.js`，56 条真实内容
- 训练日志：`history.js`（读写 `fit-history-v1`：只增不减、按日期一键、封存时间戳、月/年聚合）
- **无**真实用户数、推荐语、案例、媒体报道。

[absence: 后续任何工作都不得编造用户量、评价或数据]

## Product Principles

1. **记录要极短。** 训练中单手、注意力有限，多一步都是成本。
2. **价值来自长期累积，不来自单次使用。** 设计要为"回来看"服务，不只是"今天点完"。
3. **手机是真场景，桌面是次要场景。** 窄屏优先，不是先做宽屏再压缩。
4. **不做说服层。** 这是工具，不是落地页；没有 hero、没有 CTA、没有社会证明。
5. **中国大陆可访问是底线，不是优化项。**

## Accessibility & Inclusion

- 用户确认"必须在手机上能用" → 触摸目标尺寸和窄屏布局是功能要求，不是装饰性适配。
